# 内容 → 小红书：Codex 项目规则

本文件是本仓库的项目规则入口。不要默认全仓扫描；只按当前任务需要读取文件。

# Codex 项目硬性约束

## 记忆术语与授权范围

1. **Codex 全局记忆**：指 Codex 管理的仓库外跨项目记忆，默认位于
   `~/.codex/memories/**`。其读写遵循上层 Codex 全局记忆规则。

2. **项目 Memory Bank**：仅指当前仓库内的 `./memory-bank/**`。
   它属于项目文档，不属于 Codex 全局记忆。

3. 当用户已经授权实现、修改或执行当前项目任务时，仅与该任务直接相关、
   且本文件明确要求的最小 Memory Bank 同步，属于当前项目任务范围，
   不需要取得“更新 Codex 全局记忆”的额外授权。

4. 第 3 条不得被解释为对整个 Memory Bank 的概括授权：
   - 不得更新与当前任务无关的项目记忆；
   - 不得借同步之名整理、改写或扩展无关历史；
   - `memory-bank/archive/` 是行为隔离区，不是文件系统安全边界；
   - 默认不得扫描、搜索或读取归档内容，只有用户明确指定归档文件，
     或当前任务就是已授权的归档维护时，才允许按指定范围访问。

5. 审查、分析、复盘和计划等只读任务不得修改项目 Memory Bank。
   只读过程中发现同步或归档需求时，仅在最终回复中报告。

## 指令优先级与授权边界

1. 系统、开发者和用户当前指令高于本文件；本文件高于项目记忆和一般文档。
2. 审查、分析、复盘和计划请求保持只读；只有用户明确要求修改、实现或执行时才编辑文件。
3. 未经用户明确要求，不创建或切换分支，不提交、不推送、不发布。

## 默认工作预算与重复操作控制

第 1 项是启动读取边界，只适用其中明确列出的例外。第 2–6 项规定的是
默认工作预算，不是正确性边界；出现测试失败、工具报错、源码歧义、
用户修改、跨文件调用关系或当前任务明确要求方案比较时，可以突破默认
预算，但不得无理由重复读取、重复验证或扩大任务范围。

1. **启动读取边界**
   - 在仓库自主读取范围内，收到具体需求前只读本文件。
   - 用户要求“查看下一任务”时，按下文“下一任务发现规则”额外读取当前待办。
   - 更高优先级指令要求的上下文除外。

2. **精确读取预算**
   - 优先使用 `rg -n` 定位具体函数、变量或文件，再一次性读取完成任务所需的局部区间。
   - 同一源码文件在编辑前默认完成一次定位和一次必要上下文读取；只有出现
     测试或构建指向、工具失败、用户修改、明确歧义或新的跨文件证据时才继续读取。
   - 第一次检索默认只输出文件、行号和命中内容；需要上下文时再读取命中附近片段。
   - 多文件检索默认不同时使用超过 2 行上下文；单次源码或差异读取默认不超过
     160 行。任务正确性确实需要时可以扩大，但应先缩小目标范围。
   - 大文件先精确定位，再按小范围行号读取；输出截断后优先缩小范围，不重复
     提高上限运行同一命令。

3. **工具输出预算**
   - 普通检索单次输出默认不超过 4000 tokens；失败详情或正确性判断确实需要时
     可以提高。
   - 默认查看 `git diff --stat`、`git diff --check` 和指定文件的关键差异；
     只有当前审查确实需要完整变更上下文时才扩大差异范围。
   - 不重复输出完整测试日志或已经成功读取的内容。
   - 不为最终回复专门重新读取源码行号；文件链接默认不附行号。

4. **实现方案预算**
   - 修改前优先选择满足当前需求的最小方案。
   - 只有当前方案无法验证、存在实质性权衡或用户明确要求比较时，才评估替代方案。

5. **验证预算**
   - 开发过程中优先运行与改动直接相关的定向测试。
   - 代码和相关环境均未变化时，默认不重复运行同一完整验证；相关代码、
     测试环境或失败证据发生变化后可以重跑。
   - 修改成功后优先运行 `git diff --check`、`git diff --stat` 和对应的定向测试
     或构建，不重新输出源码。
   - 最终成功状态只保留一次完整验证结果；不得为了确认同一既有失败而重复执行。

6. **沟通预算**
   - 中间进度只报告假设、关键里程碑和阻塞，不逐项复述工具操作。
   - 编码和修改任务的最终回复优先包含：改动、验证、风险、未执行项目。
   - 审查、诊断、解释和计划任务按当前用户请求组织回复。

## 🛑 Shell 工具与文件搜索红线（严格执行）

1. **禁止递归扫描文档**：
   - 严禁无过滤执行 `rg --files`、`find` 或任何针对 `*.md` 的全局扫描。
   - 已知文件名、目录或扩展名时，允许使用带精确路径或 `-g` 限定的 `rg --files`。
   - 寻找代码时使用具体函数名、文件名、目录或扩展名限定范围，禁止无目标的通配符全仓搜索。

2. **读取阻断**：
   - 除非当前需求直接涉及对应规范或用户明确指明路径，否则**严禁读取**以下大文件：
     - `Long-form-post-prompt.md`
     - `design-*.md`

## 记忆范围说明

- Codex 全局记忆位于仓库外，其读写遵循上层全局规则。
- `memory-bank/**` 是当前仓库的项目文档，不属于 Codex 全局记忆。
- 本文件只规定项目 Memory Bank 的授权边界；具体维护流程见
  `memory-bank/README.md`。

## 项目文档加载路由

以下文件均为当前仓库的项目文档，不是 Codex 全局规则，也不会被 Codex
自动加载。只有满足触发条件时才按明确路径读取。

| 项目文档 | 强制触发条件 | 读取时机 |
|---|---|---|
| `docs/development/validation.md` | 用户已授权修改项目文件；或明确要求浏览器、视觉、移动端验收 | 第一次编辑或调用相关验证工具之前 |
| `memory-bank/README.md` | 当前任务需要读取、写入或归档 `memory-bank/**`；或用户要求查看下一任务 | 访问其他 Memory Bank 文件之前 |

## UI System v1

涉及 React、CSS、交互控件或页面布局的任务，修改前必须完整读取并遵循
`docs/ui-system.md`。该文件是应用界面的唯一视觉规范。

- 新功能必须优先复用 `src/components/ui/**` 和现有视觉 Token。
- 业务组件不得创建新的通用按钮、图标按钮、操作组或分段选择器样式。
- 应用图标统一经 `src/components/ui/icons.js` 引入。
- 现有组件不能满足需求时，优先扩展公共组件；新增视觉语言、颜色、尺寸或
  组件变体必须获得用户明确授权，并先更新 `docs/ui-system.md`。
- `npm run check:ui` 属于 UI 修改的必需验证，不得通过扩大例外绕过。

- 除非用户在当前需求中明确要求浏览器、视觉或移动端验收，否则不得调用
  浏览器、截图或视觉自动化工具；详细验证规则见
  `docs/development/validation.md`。
- 如果必需的项目文档缺失或不可读，不得自行猜测其规则；停止对应操作并
  向用户报告。
- 本文件的授权、安全和产品约束高于上述项目文档。项目文档只能补充操作
  细节，不得扩大授权、降低安全要求或修改产品约束。

## 下一任务发现规则

当用户说“查看下一任务”“看看接下来的任务”或同义表达时：

1. 先读取本文件和 `memory-bank/README.md`，再只读取
   `memory-bank/progress.md` 的“当前待办”部分。
2. 选择并复述第一个未完成项目，说明预计范围与验收结果。
3. “查看”保持只读；只有用户明确要求“执行”“实现”“修改”或“编码”时才编辑文件。
4. 不读取已完成列表、归档或其他 Memory Bank 文件，除非首个待办本身明确需要。


## 项目定位

这是一个本地运行的内容到小红书转换器：

- 输入：任意来源的文本内容，或一条可公开访问的 `http(s)` 网页 URL；X/Twitter 帖子 URL 保留专用解析。
- 输出：小红书平台「写长文」笔记。
- 不做：视频、普通图文笔记、播客或自动发布。
- Markdown 只用于模型输出和内部拆分；复制到小红书时输出清理后的纯文本。

## 不可破坏的产品约束

- 默认使用中文界面和中文回复。
- 页面只有两个核心工作区：`01 内容创作`、`02 小红书笔记`。
- 两个工作区保持同一可用高度；整页禁止上下或横向滚动，内容只能在各面板内部滚动。
- `02` 不能提供“一次复制全文”作为主流程，只展示 4 个独立复制区块：
  长文标题、长文正文、正文描述、标签。不展示“一键排版”“预览并发布”或
  其他顺序步骤。
- 全局、标题、正文、正文摘要和标签规则只存在于当前提示词方案，不得在代码中重复写死。
- 页面不执行内容通过/失败判定；模型结果不符合预期时由用户主动重新生成。
- JSON、非空结果和候选数量等技术协议必须保留，但不得扩展为内容 validation。
- 供应商降级顺序固定：Gemini → Groq Qwen → 智谱 GLM → 硅基流动 Qwen →
  OpenRouter Free。
- 每家供应商内部允许单选或有序多选模型；先按选中顺序尝试同一供应商，
  再进入下一供应商。
- API Key 只能存在本地服务端 `.env`：
  - 不写入 React 源码。
  - 不写入浏览器存储。
  - 健康检查和设置接口不得返回 Key。
  - 配置页密码框不得回显已保存 Key。
- 不要把用户密钥、提示词中的输入正文或模型完整响应写入日志、测试快照、文档或提交信息。
- 历史原文和草稿只能写入被 Git 忽略的本地数据目录，不得进入测试快照或文档。

## 权威与同步规则

| 主题 | 权威来源 | 修改时必须同步 |
|---|---|---|
| 默认提示词与内容规范 | `Long-form-post-prompt.md` | `promptStore.mjs`、提示词测试；长期契约变化时同步 `memory-bank/activeContext.md` 和 `memory-bank/progress.md` |
| 本地提示词方案 | `promptStore.mjs` | `server.mjs`、`src/ApiSettingsDialog.jsx`、测试；架构变化时同步 `memory-bank/techContext.md` |
| 供应商顺序、候选模型与降级策略 | `providers.mjs` | `src/ApiSettingsDialog.jsx`、README、测试 |
| 发布复制区块 | `src/PublishWorkflow.jsx`、`src/xiaohongshuPublish.js` | 对应测试；产品契约变化时同步 `memory-bank/productContext.md` |
| API 设置安全 | `server.mjs`、`src/ApiSettingsDialog.jsx` | `.env.example`、安全测试；架构变化时同步 `memory-bank/techContext.md` |
| 历史笔记 | `historyStore.mjs` | `server.mjs`、`src/HistoryDialog.jsx`、测试；架构变化时同步 `memory-bank/techContext.md` |

## 快速模块地图

| 文件 | 职责 |
|---|---|
| `src/App.jsx` | 两栏主界面、生成状态、健康状态、设置入口 |
| `src/ApiSettingsDialog.jsx` | 快速设置、高级提示词、API 与模型配置弹窗 |
| `src/HistoryDialog.jsx` | 历史列表、版本详情、载入和删除弹窗 |
| `src/contentPreferences.js` | 快速设置字段、默认值、白名单清洗与 Prompt 注入 |
| `src/PublishWorkflow.jsx` | 小红书 4 个复制区块与主动重新生成 |
| `promptStore.mjs` | 默认提示词解析、本地方案存储和模块组合 |
| `src/xiaohongshuPublish.js` | 把 Markdown 拆成标题、正文、描述、标签 |
| `src/xiaohongshuText.js` | 清理 Markdown 为可粘贴纯文本 |
| `providers.mjs` | 五家模型适配、错误归一化和自动降级 |
| `server.mjs` | 本地 HTTP API、X URL 解析、提示词组装、设置持久化 |
| `historyStore.mjs` | 本地历史 JSON、备份、排序、限额与原子写入 |
| `scripts/dev.mjs` | 同时启动 Node API 与 Vite |
| `tests/` | Node 原生测试，覆盖提示词方案、供应商、设置和文本拆分 |

## 提示词任务路由

- 提示词内容规则通过页面方案或 `Long-form-post-prompt.md` 调整；只有当前需求
  直接涉及提示词规范时才读取该文件。
- 修改提示词模块边界、输入注入或 JSON 协议前，按需读取
  `memory-bank/activeContext.md`。
