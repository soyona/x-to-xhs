# X → 小红书：Codex 项目规则

本文件是本仓库的项目规则入口。不要默认全仓扫描；只按当前任务需要读取文件。

# Codex 项目硬性约束

## 指令优先级与授权边界

1. 系统、开发者和用户当前指令高于本文件；本文件高于项目记忆和一般文档。
2. 审查、分析、复盘和计划请求保持只读；只有用户明确要求修改、实现或执行时才编辑文件。
3. 未经用户明确要求，不创建或切换分支，不提交、不推送、不发布。

## 🛑 Token 节约与读取红线（严格执行）

1. **启动读取边界**：在仓库自主读取范围内，收到具体需求前只读本文件；更高优先级指令要求的上下文除外。
2. **Memory Bank 严格按需读取**：
   - `memory-bank/activeContext.md`（防失忆与轮转瘦身）：只记录即时上下文、技术债与阻碍
     （Blocker）。物理行数超过 150 行时自动触发物理脱水，将冷冻切片归档至
     `memory-bank/archive/`。
   - `memory-bank/progress.md`（进度锚点）：只使用切香肠式 `[ ]` 与 `[x]` 可执行清单。
     AI 每成功编译一个特性，必须勾选对应项目；已完成任务超过 20 项时触发
     冷冻归档。
   - `memory-bank/archive/`（Codex 读取隔离区）：存放已脱水的历史看板与上下文；
     Codex 禁止扫描、搜索或读取，仅允许在触发物理脱水时写入。
3. **代码读取原则**：
   - 优先使用 `rg` 定位具体函数、变量或文件，只查看直接相关的代码片段，禁止无脑读取整个 `.jsx` / `.js` 文件。
4. **工具输出原则**：
   - 默认只保留命令汇总、失败详情和直接相关片段；禁止重复输出完整测试日志、完整 `git diff` 或已读内容。
   - 大文件先用精确检索定位，再按小范围行号读取；发生输出截断后不得用同一大范围命令重试。
5. **验证次数原则**：
   - 开发过程中只运行与改动直接相关的定向测试。
   - 代码未变化时不得重复运行同一完整验证；验证失败后，只有相关代码已经修改才允许重跑。
   - 最终成功状态只保留一次完整验证结果；已知既有失败不得为确认同一结果重复执行。
6. **沟通原则**：
   - 中间进度只报告假设、关键里程碑和阻塞，不逐项复述工具操作。
   - 最终回复优先只包含改动、验证、已知风险和未执行项。


## 🛑 Shell 工具与文件搜索红线（严格执行）

1. **禁止递归扫描文档**：
   - 严禁无过滤执行 `rg --files`、`find` 或任何针对 `*.md` 的全局扫描。
   - 已知文件名、目录或扩展名时，允许使用带精确路径或 `-g` 限定的 `rg --files`。
   - 寻找代码时使用具体函数名、文件名、目录或扩展名限定范围，禁止无目标的通配符全仓搜索。

2. **读取阻断**：
   - 除非当前需求直接涉及对应规范或用户明确指明路径，否则**严禁读取**以下大文件：
     - `Long-form-post-prompt.md`
     - `design-*.md`

## 🛑 全局记忆边界

1. 除非更高优先级指令要求或用户明确授权，否则不读写全局 `MEMORY.md`。
2. 确需更新时必须合并或替换现有项目节点，只保存稳定偏好和指针；不得写入重复 Task Group、完整日志、Prompt、代码片段或一次性环境状态。


## 项目定位

这是一个本地运行的 X-to-Xiaohongshu 转换器：

- 输入：完整 X 帖子文本，或一条 `x.com` / `twitter.com` 帖子 URL。
- 输出：小红书平台「写长文」笔记。
- 不做：视频、普通图文笔记、播客或自动发布。
- Markdown 只用于模型输出和内部拆分；复制到小红书时输出清理后的纯文本。

## 不可破坏的产品约束

- 默认使用中文界面和中文回复。
- 页面只有两个核心工作区：`01 粘贴 X 内容`、`02 小红书长文`。
- 两个工作区保持同一可用高度；整页禁止上下或横向滚动，内容只能在各面板内部滚动。
- `02` 不能提供“一次复制全文”作为主流程，必须按小红书「写长文」的 7 步流程分段处理：
  1. 长文标题
  2. 长文正文
  3. 一键排版
  4. 发布标题
  5. 正文描述
  6. 标签
  7. 预览并发布
- 全局、标题、正文、正文摘要和标签规则只存在于当前提示词方案，不得在代码中重复写死。
- 页面不执行内容通过/失败判定；模型结果不符合预期时由用户主动重新生成。
- JSON、非空结果和候选数量等技术协议必须保留，但不得扩展为内容 validation。
- 供应商降级顺序固定：Gemini → Groq Qwen → 智谱 GLM → 硅基流动 Qwen →
  OpenRouter Free。
- 每家供应商内部允许单选或有序多选模型；先按选中顺序尝试同一供应商，
  再进入下一供应商。
- API Key 只能存在本地服务端 `.env`：
  - 不写入 React 源码。
  - 不写入浏览器存储。
  - 健康检查和设置接口不得返回 Key。
  - 配置页密码框不得回显已保存 Key。
- 不要把用户密钥、提示词中的输入正文或模型完整响应写入日志、测试快照、文档或提交信息。
- 历史原文和草稿只能写入被 Git 忽略的本地数据目录，不得进入测试快照或文档。

## 权威与同步规则

| 主题 | 权威来源 | 修改时必须同步 |
|---|---|---|
| 默认提示词与内容规范 | `Long-form-post-prompt.md` | `promptStore.mjs`、提示词测试；长期契约变化时同步 `memory-bank/activeContext.md` 和 `memory-bank/progress.md` |
| 本地提示词方案 | `promptStore.mjs` | `server.mjs`、`src/ApiSettingsDialog.jsx`、测试；架构变化时同步 `memory-bank/techContext.md` |
| 供应商顺序、候选模型与降级策略 | `providers.mjs` | `src/ApiSettingsDialog.jsx`、README、测试 |
| 发布分段流程 | `src/PublishWorkflow.jsx`、`src/xiaohongshuPublish.js` | 对应测试；产品契约变化时同步 `memory-bank/productContext.md` |
| API 设置安全 | `server.mjs`、`src/ApiSettingsDialog.jsx` | `.env.example`、安全测试；架构变化时同步 `memory-bank/techContext.md` |
| 历史笔记 | `historyStore.mjs` | `server.mjs`、`src/HistoryDialog.jsx`、测试；架构变化时同步 `memory-bank/techContext.md` |

## 常用命令

要求 Node.js 20.18 或更高版本。

```bash
npm run dev
npm run check
```

- 仅在依赖缺失、`package.json` 已变化或用户明确要求时运行 `npm install`。
- 开发前端：`http://localhost:5173`
- 本地 API：`http://localhost:8787`
- 生产模式：先 `npm run build`，再 `npm start`

## 修改与验证要求

1. 修改前确认工作区状态，不覆盖用户已有改动。
2. 只做任务范围内的变更，不顺手重构无关模块。
3. 默认验证矩阵：

   | 改动类型 | 默认验证 |
   |---|---|
   | 规则、README、纯文档 | `git diff --check` |
   | 服务端或纯逻辑 | 定向测试；完成后运行完整检查 |
   | React / UI | 定向测试和生产构建；行为代码变化时运行完整检查 |
   | API Key 或安全配置 | 定向测试、完整检查和第 6 条安全检查 |

4. 验证执行与重跑：

   - 开发过程中优先运行相关测试文件。
   - 对验证矩阵要求完整检查的代码改动，在所有修改完成后运行：

   ```bash
   npm run check
   git diff --check
   ```

   - 如果完整检查因已知测试失败而在构建前停止，不重复运行同一测试套件；
     单独运行一次 `npm run build`，并准确报告既有失败。
   - 如果发现本次改动引入的失败，修复相关代码后允许重跑；代码未变化时不得
     重复运行同一完整验证。
5. 浏览器验证规则：
   - 默认不调用浏览器，不进行浏览器自动化或截图验证。
   - UI 改动默认只执行单元测试、静态检查和生产构建。
   - 只有用户在当前需求中明确要求“浏览器验证”“视觉验收”或“移动端验收”时，才允许调用浏览器。
   - 未执行浏览器验证时，应明确报告：仅完成静态、测试和构建验证，浏览器交互尚未验收。
   - 在不与系统、开发者或用户当前指令冲突的前提下，本项目默认规则优先于
     一般技能建议。
6. 涉及 API Key 时，额外验证：
   - `.env` 仍被 Git 忽略。
   - 文件权限保持 `600`。
   - `/api/health` 不包含 `apiKey`、`secret` 或 `token` 字段。
## 快速模块地图

| 文件 | 职责 |
|---|---|
| `src/App.jsx` | 两栏主界面、生成状态、健康状态、设置入口 |
| `src/ApiSettingsDialog.jsx` | 快速设置、高级提示词、API 与模型配置弹窗 |
| `src/HistoryDialog.jsx` | 历史列表、版本详情、载入和删除弹窗 |
| `src/contentPreferences.js` | 快速设置字段、默认值、白名单清洗与 Prompt 注入 |
| `src/PublishWorkflow.jsx` | 小红书 7 步发布、分段复制与主动重新生成 |
| `promptStore.mjs` | 默认提示词解析、本地方案存储和模块组合 |
| `src/xiaohongshuPublish.js` | 把 Markdown 拆成标题、正文、描述、标签 |
| `src/xiaohongshuText.js` | 清理 Markdown 为可粘贴纯文本 |
| `providers.mjs` | 五家模型适配、错误归一化和自动降级 |
| `server.mjs` | 本地 HTTP API、X URL 解析、提示词组装、设置持久化 |
| `historyStore.mjs` | 本地历史 JSON、备份、排序、限额与原子写入 |
| `scripts/dev.mjs` | 同时启动 Node API 与 Vite |
| `tests/` | Node 原生测试，覆盖提示词方案、供应商、设置和文本拆分 |

## 提示词任务路由

- 提示词内容规则通过页面方案或 `Long-form-post-prompt.md` 调整；只有当前需求
  直接涉及提示词规范时才读取该文件。
- 修改提示词模块边界、输入注入或 JSON 协议前，按需读取
  `memory-bank/activeContext.md`。
