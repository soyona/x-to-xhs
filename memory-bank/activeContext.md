# Active Context

更新日期：2026-07-29

## 当前状态

项目是一个本地运行的 X → 小红书长文工作流：

- 支持原帖链接、复制的 X 原文和自主编写内容三种来源模式。
- 明确的 X URL 自动识别；无链接纯文本要求用户确认来源。
- Gemini → Groq Qwen → 智谱 GLM → 硅基流动 Qwen →
  OpenRouter Free 自动降级。
- `02 小红书长文`按 7 步流程拆分标题、正文、发布标题、正文描述和标签。
- 正文支持富文本复制、纯文本兜底和 Markdown 导出。
- 标题、正文、正文描述和标签支持主动重新生成；采用前保留当前内容。
- 本地历史支持查看、载入和删除。
- API Key 只保存在服务端 `.env`。

## 提示词架构

- `Long-form-post-prompt.md` 是系统默认提示词。
- 默认文件包含六个模块：`global`、`title`、`body`、`summary`、`tags`、
  `output`。
- `promptStore.mjs` 每次读取默认文件；直接修改后，下次生成立即生效。
- 页面“创作设置”包含快速设置和高级提示词；高级提示词开放全局、标题、
  正文、正文摘要和标签五个模块。
- 页面自定义方案保存在 `.local-data/prompts.json`，目录权限为 `700`，
  文件权限为 `600`。
- 自定义方案可覆盖全局与四个内容模块；固定输出协议继承默认模板。
- 完整生成与局部重新生成使用同一个当前方案。
- 快速设置由浏览器本地保存，经服务端白名单清洗后注入提示词。

## 内容处理边界

- 内容规则完全由当前提示词方案负责，不在代码中重复写死。
- 原帖链接与 X 原文使用公开内容解读模式；自主编写内容使用原创编辑模式。
- 来源元数据由服务端结构化保存，正文署名在复制与导出阶段确定性附加，
  不依赖模型生成。
- X 原文缺少链接时允许生成草稿，但输入区和发布步骤持续提示补充来源。
- 页面不执行标题字数、Emoji、章节、摘要长度或标签数量等内容 validation。
- 模型内容不符合提示词时仍返回页面，由用户判断并主动重新生成。
- 系统只保留技术协议检查：JSON 可解析、候选非空、候选数量正确。
- 已移除 `src/validation.js`、`src/repairStrategy.js`、
  `src/RepairControls.jsx` 和 `/api/repair`。
- 新稿不再包含`【审稿自查】`；拆分层继续兼容包含该分区的旧历史稿。

## 默认提示词摘要

- 标题默认 16–20 字、恰好 1 个 Emoji，并自然包含“超全干货”。
- 只有正文实际超过 8000 字才允许使用“万字深度”。
- 正文按素材信息量自然展开，不设最低字数，不超过 10000 字。
- 摘要默认 220–280 字并使用三块结构。
- 标签默认 8 个，不声称使用了不存在的实时趋势数据。
- 固定尾部只包含`正文小结 / 摘要`和`推荐标签`。

## 当前 UX

- 设置弹窗包含“创作设置”和“模型与 API”。
- 创作设置使用“快速设置 / 高级提示词”二级切换，始终完整展示一个工作区。
- 设置弹窗保持稳定工作区高度；高级提示词内容区独立滚动，底部操作保持可见。
- 高级提示词支持选择方案、新建副本、编辑五个模块、保存、删除和恢复模块默认值。
- 旧版补充创作指令继续兼容，并可一键迁移到当前方案的全局规则。
- 系统默认方案来自受版本管理的 `Long-form-post-prompt.md`；页面保存会创建本地
  自定义方案，不直接修改默认文件。
- `02` 顶部仅说明内容由当前提示词控制，不显示通过数、失败数或修复入口。
- 第 7 步为“预览并发布”；正文变化后继续提示发布标题、描述和标签可能过期。
- 输入区自动识别明确的 X URL；纯文本通过两个轻量按钮确认“X 原文”或
  “自主编写”。
- 第 2 步正文预览、富文本复制和 Markdown 导出使用同一来源落款；
  自主编写模式只保留 AI 辅助说明。
- 第 7 步按来源模式提示核验作者、原帖链接、引用、事实和 AI 内容声明。

## 验证边界

- 来源识别、来源传递、历史保存、确定性署名和 Markdown 导出已有单元测试。
- `npm run build` 已通过。
- 提示词模块解析、自定义方案覆盖、默认文件实时更新和局部生成技术协议已有测试。
- 完整测试仍有仓库既有的 `examples/demo1.md` 缺失夹具失败；不得把它报告为绿灯。
- 页面创作设置已完成桌面和 390px 移动端浏览器交互验收。

## 工作区提醒

- `.env` 包含本地密钥，永远不要读取后原样输出或加入 Git。
- `.local-data/` 被 Git 忽略，不得提交其中的历史稿或自定义提示词。
- 修改提示词模块标记、输入注入或 JSON 返回协议时，必须同步
  `promptStore.mjs` 和相关测试。
