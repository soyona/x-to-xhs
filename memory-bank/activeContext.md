# Active Context

更新日期：2026-07-25

## 当前状态

项目已具备完整的本地 MVP：

- X 文本或单条 URL 输入。
- X oEmbed 正文解析与失败提示。
- Gemini → Groq Qwen → OpenRouter Free 自动降级。
- 按 Markdown 规范生成小红书「写长文」草稿。
- 按小红书 7 步流程拆分和复制。
- 11 项自动规范检查，已映射到对应发布步骤。
- 8 项人工内容复核，集中在第 7 步并支持逐项勾选。
- 自动修复、结构化修订、整篇重生、最多两次处理和版本恢复。
- 右上角本地 API Key / 模型配置。
- 可配置并在浏览器本地保存创作偏好，生成与修复使用同一份偏好。
- 桌面端两栏、移动端两段固定视口布局，整页无滚动。
- 共有 34 项测试；最新运行 33 项通过，1 项因
  `examples/demo1.md` 测试夹具缺失而失败；生产构建通过。

## 最近完成

1. `02 小红书长文`展示唯一的结构化正文源稿，复制时从同一字段确定性
   派生富文本和无井号纯文本兜底。
2. 修正长文篇幅合同：正文不设最低字数，按原文信息量自然展开，只执行
   10000 字平台上限。
3. 修正卡片切分认知：分页由小红书“一键排版”决定，提示词禁止输出
   `---` 和文字版页脚，兼容清理也会移除旧稿中的这两类内容。
4. 正文改为保留`#`、`##`、`-`、`1.`的结构化复制文本，供小红书
   识别 H1、H2、无序列表和有序列表。
5. 正文复制同时写入`text/html`富文本和无井号`text/plain`兜底；
   平台支持富文本时接收标题/列表语义，只接受纯文本时不再显示井号。
6. Markdown 清理降为模型偏离规范时的兼容层，并保留目标层级标记、
   Unicode 符号、Emoji 和连续空行。
7. 合并规则入口：删除重复的 `AGENTS.md`，只保留 `.codexrules`。
8. `03 规范检查` 按 Markdown 父子关系重新分组。
9. 明确显示每个自动检查的条件。
10. 增加人工内容复核组，且不计入自动失败。
11. 强化固定格式检查，禁止固定尾部分区之间出现额外标题。
12. 增加右上角 API 配置弹窗：
   - Key 不回显。
   - 支持保留、替换和清除。
   - 模型名称可编辑。
   - 占位符不会被误判为已配置。
   - `.env` 原子写入并保持 `600` 权限。
13. 取消独立的 `03 规范检查`，将检查结果融合到 `02` 对应发布步骤。
14. 增加整稿汇总、局部“修复此部分”、修复中、通过、失败和版本恢复状态。
15. 增加仅开发环境使用的 `?prototype=1` 本地交互稿，不调用模型即可体验失败、修复、通过和恢复。
16. 增加结构化创作偏好：
   - 目标读者、内容目标、语气、深度、术语、案例、Emoji 和行动建议。
   - 作者表达身份、禁用表达和 500 字补充指令。
   - 服务端白名单清洗，不能覆盖固定输出协议。
   - 设置弹窗分为“创作偏好”和“模型与 API”。

## 已知问题：优先级高

### 测试夹具缺失

`tests/xiaohongshuText.test.js` 依赖 `examples/demo1.md`。该文件当前不在
工作区，导致 `npm run test` 的 demo1 转换测试出现 `ENOENT`。

不要凭空重建示例内容；应从用户认可的原始文件恢复，或由用户授权后把测试改成
仓库内稳定的最小夹具。

### 修复问题描述不够细

`structure`、`fixed-format`、`tag-format` 等复合检查目前只给出“不完整”或“不正确”，没有说明具体缺失条件。

推荐改造：

```js
{
  actual: "缺少结尾",
  problem: "正文没有识别到结尾分区",
  repairInstruction: "保留现有正文，在实战落地后补充价值总结、趋势思辨和2–3个开放问题"
}
```

### 单个结构失败会触发整篇重生

`repairStrategy.js` 把任何 `structure` 失败都视为严重失败。即使其他十项通过、只缺少结尾，也会选择 `regenerate`。

期望：

- 单独缺少结尾、总览或一个主体标题：局部修复。
- 多个核心分区缺失或正文严重不足：整篇重生。

### 局部修复仍返回完整草稿

界面已经能从对应部分发起修复，并只把该部分失败项作为重点目标提交；但模型接口
仍返回完整 Markdown。当前按钮使用“修复此部分”，没有承诺只替换单个字段。
如果未来改为“重新生成本部分”，服务端必须提供真正的分区锁定与合并协议。

## 建议下一步

1. 为复合检查增加 `problem`、`missingReasons`、`repairInstruction`。
2. 重新定义 `repair` / `revise` / `regenerate` 的严重度规则。
3. 为局部修复增加服务端分区锁定与合并协议。
4. 增加场景测试：
   - 只缺结尾时不得整篇重生。
   - 只多一个额外分区时只删除该分区。
   - 只超标题字数时只改标题。
   - 第二次处理只提交剩余失败项。

## 工作区提醒

- 当前仓库存在用户已有改动和未跟踪文件，不要执行破坏性 Git 操作。
- `Long-form-post-prompt.md` 已有修改；修改内容规范前必须先查看当前差异。
- `.env` 包含本地密钥，永远不要读取后原样输出或加入 Git。
- 运行中的开发服务可能需要重启才能读取服务端代码变更。
